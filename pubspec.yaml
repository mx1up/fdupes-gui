name: fdupes_gui
description: fdupes front-end
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  process_run: ^0.13.2
  file_selector: ^1.0.1
  flutter_bloc: ^8.1.3
  url_launcher: ^6.2.1
  open_file: ^3.3.2
  path: ^1.8.3
  adaptive_theme: ^3.4.1
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon_256.png
